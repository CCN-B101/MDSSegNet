# Pretrained weights

This directory contains the released MDSSegNet weights for the Crack500, CTC, and DeepCrack datasets.

The same files are also available from the [`weights-v1.0`](../../releases/tag/weights-v1.0) release.
